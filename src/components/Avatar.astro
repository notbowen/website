---
import { cn } from "@lib/utils";

type Props = {
  link: string;
  className?: string;
};

const { link, className, ...rest } = Astro.props;
---

<img src={link} {...rest} class={cn("rounded-full", className)} />
